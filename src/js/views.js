function balanceHeader(){const c=current(); const lvl=lastPetrolBars(); const barsHtml=lvl!==null?miniBarGauge(lvl):'—';
return `<div class="balance-card nav-fuel-card" onclick="setTab('gas')"><div class="balance-label">🛢️ Газ</div><div class="balance-line"><div class="balance-value">${fmt(c.gas,1)} л</div><button data-action="editBalance" data-value="gas" onclick="event.stopPropagation()">Змінити</button></div></div><div class="balance-card nav-fuel-card" onclick="setTab('petrol')"><div class="balance-label">⛽ Бензин</div><div class="balance-line" style="align-items:center;gap:6px;min-height:38px">${barsHtml}</div></div><div class="balance-card"><div class="balance-label">🚗 Пробіг</div><div class="balance-line"><div class="balance-value">${mileageFmt(c.odometer)} км</div><button data-action="editBalance" data-value="odometer">Змінити</button></div></div>`}
function lastTripMode(){const t=[...state.trips].sort((a,b)=>new Date(b.date)-new Date(a.date)||new Date(b.createdAt||0)-new Date(a.createdAt||0))[0];return t?.mode==='road'?'road':'city'}
function tripButtonClass(){return lastTripMode()==='road'?'trip-road':'trip-city'}
function syncUiChrome(){const tb=document.querySelector('.topbar');if(tb)tb.classList.remove('is-hidden');const bh=document.getElementById('balanceHeader');if(bh)bh.style.display=(state.activeTab==='gas'||state.activeTab==='petrol')?'':'none';const fab=document.querySelector('.fab.trip');if(fab){const road=lastTripMode()==='road';fab.classList.toggle('road',road);fab.classList.toggle('city',!road)}}
function gasPage(){
  const s=gasStats(), c2=current(), mode=state.settings.dashboardMode||'city', mainKm=mode==='road'?s.roadKm:s.cityKm, altKm=mode==='road'?s.cityKm:s.roadKm, altLabel=mode==='road'?'по місту':'по трасі', mainClass=mode==='road'?'road':'city', pColor=progressColor(s.percent);
  const odoMode=state._odoMode||'city';
  const todayStr=today();
  const yest=new Date(); yest.setDate(yest.getDate()-1); const yesterdayStr=yest.toISOString().slice(0,10);
  const recentTrips=[...state.trips].sort((a,b)=>new Date(b.date)-new Date(a.date)||new Date(b.createdAt)-new Date(a.createdAt)).filter(t=>t.date===todayStr||t.date===yesterdayStr);
  const hasOlder=state.trips.some(t=>t.date<yesterdayStr);
  return `<div class="grid">
<section class="card col-8"><div class="row between" style="margin-bottom:12px"><div><h2>🛢️ Газ</h2><div class="muted">Баланс газу можна вручну коригувати вгорі.</div></div><button class="btn good" data-action="openGasRefuel">+ Заправка</button></div>
<div class="odo-widget"><div class="odo-label" style="font-size:12px;font-weight:700;text-transform:uppercase;color:var(--muted);letter-spacing:.03em;margin-bottom:4px">🚗 Пробіг</div><div class="odo-display" id="odoDisplay">${mileageFmt(s.odometer)} км</div><div class="odo-controls"><button class="odo-btn minus" id="odoMinus">−</button><div style="text-align:center"><div style="font-size:13px;color:var(--muted);font-weight:700" id="odoDelta">+0 км</div></div><button class="odo-btn plus" id="odoPlus">+</button></div><div class="odo-mode"><button class="${odoMode==='city'?'active':''}" id="odoModeCity" onclick="setOdoMode('city')">🟡 Місто</button><button class="${odoMode==='road'?'active':''}" id="odoModeRoad" onclick="setOdoMode('road')">🔵 Траса</button></div><button class="odo-confirm ${odoMode}" id="odoConfirm">Підтвердити поїздку</button></div>
${s.warn?`<div class="danger-note">⚠️ Газу лишилося мало. Варто планувати заправку.</div>`:''}<div class="dash ${mainClass}" style="margin-top:14px"><div class="row between"><span class="pill">${mode==='road'?'🔵 По трасі':'🟡 По місту'}</span><div class="mode-switch" role="group"><button type="button" class="${mode==='city'?'active ':''}city" data-action="setDashboardMode" data-value="city">Місто</button><button type="button" class="${mode==='road'?'active ':''}road" data-action="setDashboardMode" data-value="road">Траса</button></div></div><div class="main-number">${fmt(mainKm,0)} <span class="unit">км</span></div><div class="dash-sub">Ще ${fmt(altKm,0)} км ${altLabel} · лишилося ${fmt(s.left,1)} л · ${fmt(s.percent,0)}%</div><div class="progress-wrap"><div class="progress" style="width:${s.percent}%;--progress-color:${pColor}"></div></div></div><div class="stats"><div class="stat ${mode==='road'?'city':'road'}"><div class="value">${fmt(altKm,0)} км</div><div class="label">ще ${altLabel}</div></div><div class="stat gas"><div class="value">${fmt(s.left,1)} л</div><div class="label">залишок газу</div></div><div class="stat"><div class="value">${mileageFmt(mode==='road'?s.refuelRoadAt:s.refuelCityAt)}</div><div class="label">заправка орієнтовно на км</div></div></div>
<div class="balance-grid" style="margin-top:14px"><div class="balance-card"><div class="balance-label">🛢️ Газ</div><div class="balance-line"><div class="balance-value">${fmt(c2.gas,1)} л</div><button data-action="editBalance" data-value="gas">Змінити</button></div></div><div class="balance-card"><div class="balance-label">⛽ Бензин</div><div class="balance-line"><div class="balance-value">${fmt(c2.petrol,1)} л</div><button data-action="editBalance" data-value="petrol">Змінити</button></div></div><div class="balance-card"><div class="balance-label">🚗 Пробіг</div><div class="balance-line"><div class="balance-value">${mileageFmt(c2.odometer)} км</div><button data-action="editBalance" data-value="odometer">Змінити</button></div></div></div>
</section>
<section class="card col-4"><h2>💸 Вартість</h2><div class="price-row"><div>${labelHelp('Ціна за 1 л газу','Ця ціна підставиться в наступну газову заправку і використовується для розрахунку вартості кілометра.')}<input id="gasQuickPrice" inputmode="decimal" value="${fmt(state.settings.lastGasPrice,1)}" placeholder="47,0"></div><button class="btn secondary" data-action="saveGasPrice">ОК</button></div><div class="stat city" style="margin-top:12px;margin-bottom:10px"><div class="value">${money(s.cityCost1)}</div><div class="label">1 км містом</div></div><div class="stat road"><div class="value">${money(s.roadCost1)}</div><div class="label">1 км трасою</div></div></section>
<section class="card col-12" id="quickTripsBlock"><div class="row between" style="margin-bottom:12px"><div><h2>🧭 Поїздки</h2><div class="muted">Сьогодні та вчора. Вся історія — у вкладці Історія.</div></div></div><div class="list">${recentTrips.length?recentTrips.map(tripItemHtml).join(''):`<div class="muted">Сьогодні й учора поїздок ще немає.</div>`}</div>${hasOlder?`<div class="hist-load-more"><button data-action="goHistory">📒 Вся історія поїздок</button></div>`:''}</section></div>`;
}
function lastPetrolBars(){const lvls=(state.petrolLevels||[]);if(!lvls.length)return null;return [...lvls].sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt))[0].bars}
function levelColor(bars){if(bars<=2)return '#ef4444';if(bars===3)return '#f97316';if(bars===4)return '#f5c842';if(bars<=6)return '#60a5fa';return '#22c55e'}
function levelBorder(bars){if(bars<=2)return '#dc2626';if(bars===3)return '#ea580c';if(bars===4)return '#d4a017';if(bars<=6)return '#3b82f6';return '#16a34a'}
function barColor(i,bars){return i<=bars?levelColor(bars):'var(--soft)'}
function barBorder(i,bars){return i<=bars?levelBorder(bars):'var(--line)'}
function miniBarGauge(bars){const segs=[];for(let i=1;i<=9;i++){const f=i<=bars;segs.push(`<span style="display:inline-block;width:13px;height:10px;border-radius:3px;border:1.5px solid ${barBorder(i,bars)};background:${barColor(i,bars)};margin:0 1px;flex-shrink:0"></span>`)}return `<span style="display:inline-flex;flex-direction:row;gap:0;align-items:center;flex-wrap:nowrap">${segs.join('')}</span><b style="margin-left:6px;font-size:14px">${bars}/9</b>`}
function vertBarGauge(bars,interactive=false){
  const segs=[];
  for(let i=1;i<=9;i++){
    const f=i<=bars;
    const bg=barColor(i,bars);
    const bd=barBorder(i,bars);
    const click=interactive?`data-action="setPetrolBar" data-value="${i}"`:'';
    segs.push(`<div class="bar-seg" style="background:${bg};border-color:${bd}" ${click} title="${i} паличок"></div>`);
  }
  const label=bars===0?'Порожньо':bars===1?'🔴 Зовсім мало':bars===2?'🔴 Мало':bars===3?'🟠 Маловато':bars===4?'🟡 Нормально':bars<=6?'🟢 Більш половини':bars<=8?'🟢 Майже повний':'🟢 Повний';
  return `<div class="bars-gauge"><div class="bars-column">${segs.join('')}</div><div class="bars-label">${label}</div></div>`;
}

function petrolKmPerBar(){const lvls=[...(state.petrolLevels||[])].sort((a,b)=>new Date(a.createdAt)-new Date(b.createdAt));if(lvls.length<2)return null;const segments=[];for(let i=1;i<lvls.length;i++){const prev=lvls[i-1],cur=lvls[i];const barsDiff=prev.bars-cur.bars;if(barsDiff<=0)continue;const t1=new Date(prev.createdAt),t2=new Date(cur.createdAt);const tripsInRange=state.trips.filter(t=>{const tc=new Date(t.createdAt||t.date);return tc>=t1&&tc<=t2});const km=tripsInRange.reduce((a,t)=>a+parseNum(t.km),0);if(km>0)segments.push({km,bars:barsDiff,kmpb:km/barsDiff})}if(!segments.length)return null;const totalKm=segments.reduce((a,s)=>a+s.km,0);const totalBars=segments.reduce((a,s)=>a+s.bars,0);return Math.round(totalKm/totalBars)}
function petrolLevelItemHtml(lv){const colorDot=lv.bars<=2?'🔴':lv.bars<=4?'🟡':'🟢';return `<div class="item petrol-level"><div class="item-main"><div class="item-title">${colorDot} Рівень датчика · ${lv.bars}/9 паличок</div><div class="item-sub">${esc(lv.date)}${lv.comment?' · '+esc(lv.comment):''}</div></div><div class="row" style="flex-wrap:nowrap"><button class="mini-btn danger" data-action="deletePetrolLevel" data-id="${lv.id}">Видалити</button></div></div>`}
function petrolPage(){
  const entries=state.history.filter(e=>e.fuel==='petrol');
  const total=entries.reduce((a,e)=>a+parseNum(e.sum),0);
  const totalGas=state.history.filter(e=>e.fuel==='gas').reduce((a,e)=>a+parseNum(e.sum),0);
  const bars=lastPetrolBars();
  const kmpb=petrolKmPerBar();
  const kmLeft=bars!==null&&kmpb?Math.round(bars*kmpb):null;
  const lvls=[...(state.petrolLevels||[])].sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
  const allPetrolEvents=[...entries.map(e=>({...e,_type:'refuel'})),...lvls.map(l=>({...l,_type:'level'}))].sort((a,b)=>new Date(b.createdAt||b.date)-new Date(a.createdAt||a.date));
  const gaugeHtml=vertBarGauge(bars!==null?bars:0,false);
  const lvlCount=(state.petrolLevels||[]).length;
  const kmLeftColor=bars!==null&&bars<=2?'#ef4444':bars===3?'#f97316':'inherit';
  const kmpbBlock=kmpb
    ?`<div class="kmpb-block"><div class="kmpb-val">${kmpb} км</div><div class="kmpb-label">на 1 паличку <span style="font-size:11px;color:var(--muted)">(${lvlCount} фікс.)</span></div></div>
       <div class="kmpb-block"><div class="kmpb-val" style="color:${kmLeftColor}">~${kmLeft} км</div><div class="kmpb-label">орієнтовно до порожнього</div></div>`
    :`<div class="kmpb-block" style="opacity:.65"><div class="kmpb-val">—</div><div class="kmpb-label">потрібно ≥2 фіксації рівня</div></div>`;
  return `<div class="grid"><section class="card col-12">
<div class="row between" style="margin-bottom:14px"><div><h2>⛽ Бензин</h2><div class="muted">Датчик рівня (9 паличок). Заправки — для витрат.</div></div><div class="row"><button class="btn secondary" data-action="openPetrolLevel">📊 Рівень</button><button class="btn good" data-action="openPetrolRefuel">+ Заправка</button></div></div>
<div class="bars-card"><div style="flex-shrink:0">${gaugeHtml}</div><div class="bars-info">
${kmpbBlock}
<div class="kmpb-block"><div class="kmpb-val">${money(total)}</div><div class="kmpb-label">витрачено на бензин</div></div>
<div class="kmpb-block"><div class="kmpb-val">${money(total+totalGas)}</div><div class="kmpb-label">газ + бензин разом</div></div>
</div></div>
<div style="margin-top:12px;display:flex;gap:8px;flex-wrap:wrap;align-items:center">
<button class="btn secondary" data-action="recalcPetrolKmpb" style="font-size:13px">🔄 Перерахувати</button>
<span class="muted" style="font-size:12px">оновлює км/паличку по всіх фіксаціях</span>
</div>
<div style="margin-top:16px"><h3 style="font-size:15px;margin:0 0 10px;color:var(--muted)">Всі записи</h3>
<div class="list">${allPetrolEvents.length?allPetrolEvents.map(x=>x._type==='level'?petrolLevelItemHtml(x):refuelItemHtml(x)).join(''):`<div class="muted">Ще немає записів. Додай рівень датчика або заправку.</div>`}</div></div>
<details style="margin-top:20px;border:1px solid var(--line);border-radius:18px;padding:14px"><summary style="cursor:pointer;font-weight:700;color:var(--muted);font-size:13px">🌀 Сезонне скидання статистики</summary>
<p class="muted" style="font-size:13px;margin:10px 0">Скидає всі фіксації рівня датчика (і розрахунок км/паличку). Заправки та витрати не видаляються. Використовуй при зміні сезону — влітку, восени, взимку витрата різна.</p>
<button class="btn danger" data-action="resetPetrolStats" style="margin-top:4px">Скинути фіксації рівня</button>
</details>
</section></div>`;
}

function commentHtml(c){return `<details class="comment-box"><summary>Коментар</summary><div>${esc(c)}</div></details>`}
function tripItemHtml(t){const comp=recompute(); const od=comp.tripAfter[t.id]||t.odometerAfter||0; return `<div class="item ${t.mode}"><div class="item-main"><div class="item-title">${t.mode==='road'?'🔵 Траса':'🟡 Місто'} · ${fmt(t.km,1)} км</div><div class="item-sub">${esc(t.date)} · пробіг після: ${mileageFmt(od)} км</div>${t.comment?commentHtml(t.comment):''}</div><div class="row" style="flex-wrap:nowrap"><button class="mini-btn" data-action="editTrip" data-id="${t.id}">Ред.</button><button class="mini-btn danger" data-action="deleteTrip" data-id="${t.id}">Видалити</button></div></div>`}
function refuelItemHtml(e){return `<div class="item ${e.fuel==='gas'?'gas':'petrol'}"><div class="item-main"><div class="item-title">🟢 ${e.fuel==='gas'?'Газ':'Бензин'} · ${fmt(e.liters,1)} л · ${money(e.sum)}</div><div class="item-sub">Пробіг: ${mileageFmt(e.mileage)} км · ціна: ${fmt(e.price,1)} грн/л ${e.fullTank?'· повний бак':''}</div>${e.comment?commentHtml(e.comment):''}</div><div class="row" style="flex-wrap:nowrap"><button class="mini-btn" data-action="editEntry" data-id="${e.id}">Ред.</button><button class="mini-btn danger" data-action="deleteEntry" data-id="${e.id}">Видалити</button></div></div>`}
function correctionItemHtml(x){const lab=x.kind==='gas'?'🛢️ Корекція газу':x.kind==='petrol'?'⛽ Корекція бензину':'🚗 Корекція пробігу'; const unit=x.kind==='odometer'?'км':'л'; return `<div class="item correction"><div class="item-main"><div class="item-title">${lab} · ${x.kind==='odometer'?mileageFmt(x.value):fmt(x.value,1)} ${unit}</div><div class="item-sub">${esc(x.date)} · ручне уточнення балансу</div>${x.comment?commentHtml(x.comment):''}</div><div class="row" style="flex-wrap:nowrap"><button class="mini-btn" data-action="editCorrection" data-id="${x.id}">Ред.</button><button class="mini-btn danger" data-action="deleteCorrection" data-id="${x.id}">Видалити</button></div></div>`}
function allEventsChrono(){const ev=[...state.history.map(e=>({...e,type:'refuel',sortTime:new Date(e.createdAt||e.date).getTime()})),...state.trips.map(t=>({...t,type:'trip',sortTime:new Date(t.createdAt||t.date).getTime()})),...(state.corrections||[]).map(c=>({...c,type:'correction',sortTime:new Date(c.createdAt||c.date).getTime()}))]; return ev.sort((a,b)=>new Date(b.date)-new Date(a.date)||b.sortTime-a.sortTime)}
function historyEventHtml(x){if(x.type==='refuel')return refuelItemHtml(x); if(x.type==='correction')return correctionItemHtml(x); return tripItemHtml(x)}
let _historyWeeksShown=2;
function calcStats(){const price=parseNum(state.calc?.price)||parseNum(state.settings.lastGasPrice), cityCons=parseNum(state.settings.gasCity), roadCons=parseNum(state.settings.gasRoad), multiplier=state.calc?.roundTrip?2:1; const segs=state.calc?.segments||[]; const cityKmRaw=segs.filter(s=>s.mode!=='road').reduce((a,s)=>a+parseNum(s.km),0), roadKmRaw=segs.filter(s=>s.mode==='road').reduce((a,s)=>a+parseNum(s.km),0), cityKm=cityKmRaw*multiplier, roadKm=roadKmRaw*multiplier, totalKm=cityKm+roadKm, liters=cityKm*cityCons/100+roadKm*roadCons/100, totalCost=liters*price, left=current().gas, tank=Math.max(.1,parseNum(state.settings.gasTankCapacity)), missing=Math.max(0,liters-left), refuels=liters>0?Math.ceil(missing/tank):0; const people=state.calc?.shared?Math.max(1,parseInt(state.calc.passengers||1)+(state.calc.includeDriver?1:0)):1; return {price,segs,multiplier,cityKmRaw,roadKmRaw,cityKm,roadKm,totalKm,liters,totalCost,left,tank,missing,refuels,people,perPerson:totalCost/people}}
function calcPage(){const c=calcStats(); const routeNote=state.calc?.roundTrip?`Увімкнено “туди й назад”: ${fmt(c.cityKmRaw+c.roadKmRaw,1)} км × 2 = ${fmt(c.totalKm,1)} км.`:'Маршрут рахується в один бік.'; const refuelText=c.liters<=0?'Додай відрізки, щоб побачити рекомендацію.':(c.refuels<=0?`Поточного залишку газу має вистачити. У баку зараз ≈ ${fmt(c.left,1)} л.`:`Потрібно буде заправитися приблизно ${c.refuels} раз(и). Не вистачає ≈ ${fmt(c.missing,1)} л понад поточний залишок.`); return `<div class="grid"><section class="card col-12"><h2>🧮 Калькулятор поїздки</h2><div class="muted">Разовий розрахунок маршруту. Не додає поїздки, не змінює історію, статистику або загальну ціну пального.</div></section><section class="card col-6"><h2>Маршрут</h2><div class="form-grid"><div><label>Кілометри</label><input id="calcKm" inputmode="decimal" placeholder="50"></div><div><label>Режим</label><select id="calcMode"><option value="city">Місто</option><option value="road">Траса</option></select></div><div class="full checkline"><input id="calcRoundTrip" type="checkbox" ${state.calc?.roundTrip?'checked':''}><span>Туди й назад</span></div><div class="full"><button class="btn full good" data-action="addCalcSegment">Додати відрізок</button></div></div><div class="price-note">${routeNote}</div><div class="list">${c.segs.length?c.segs.map(calcSegmentHtml).join(''):`<div class="muted">Додай кілька відрізків: місто → траса → місто тощо.</div>`}</div><div class="row" style="margin-top:12px"><button class="btn secondary" data-action="clearCalc">Очистити калькулятор</button></div></section><section class="card col-6"><h2>Ціна і спільна поїздка</h2><div class="form-grid"><div class="full"><label>Ціна за 1 л газу тільки для калькулятора</label><input id="calcPrice" inputmode="decimal" value="${fmt(c.price,1)}" placeholder="${fmt(state.settings.lastGasPrice,1)}"></div><div class="full checkline"><input id="calcShared" type="checkbox" ${state.calc?.shared?'checked':''}><span>Спільна поїздка</span></div><div><label>Пасажири</label><select id="calcPassengers"><option value="1" ${state.calc?.passengers==1?'selected':''}>1</option><option value="2" ${state.calc?.passengers==2?'selected':''}>2</option><option value="3" ${state.calc?.passengers==3?'selected':''}>3</option><option value="4" ${state.calc?.passengers==4?'selected':''}>4</option></select></div><div class="checkline" style="align-self:end"><input id="calcIncludeDriver" type="checkbox" ${state.calc?.includeDriver?'checked':''}><span>З водієм</span></div><div class="full"><button class="btn secondary full" data-action="updateCalcOptions">Оновити розрахунок</button></div></div><div class="dash gas" style="margin-top:14px"><span class="pill">💸 Головний результат</span><div class="main-number">${money(c.totalCost)}</div><div class="dash-sub">${state.calc?.shared?`≈ ${money(c.perPerson)} з людини · `:''}${fmt(c.totalKm,1)} км · ${fmt(c.liters,1)} л газу</div></div><div class="stats"><div class="stat"><div class="value">${fmt(c.totalKm,1)} км</div><div class="label">маршрут разом</div></div><div class="stat gas"><div class="value">${fmt(c.liters,1)} л</div><div class="label">газу потрібно</div></div><div class="stat"><div class="value">${money(c.totalCost)}</div><div class="label">вартість поїздки</div></div></div>${state.calc?.shared?`<div class="warning" style="margin-top:12px">Разом людей у розрахунку: ${c.people}. Орієнтовно з кожного: <b>${money(c.perPerson)}</b>.</div>`:''}</section><section class="card col-12"><h2>Рекомендація по заправках</h2><div class="stats"><div class="stat gas"><div class="value">${fmt(c.left,1)} л</div><div class="label">зараз доступно в баку</div></div><div class="stat"><div class="value">${c.refuels}</div><div class="label">заправок у дорозі</div></div><div class="stat"><div class="value">${fmt(c.missing,1)} л</div><div class="label">може не вистачити</div></div></div><div class="price-note">${refuelText}</div></section></div>`}
function historyPage(){
  const ev=allEventsChrono(), groups={};
  ev.forEach(x=>{const d=x.date||today();(groups[d]||(groups[d]=[])).push(x)});
  const days=Object.keys(groups).sort((a,b)=>new Date(b)-new Date(a));
  const cutoff=new Date(); cutoff.setDate(cutoff.getDate()-_historyWeeksShown*7);
  const cutStr=cutoff.toISOString().slice(0,10);
  const visibleDays=days.filter(d=>d>=cutStr);
  const hasMore=days.some(d=>d<cutStr);
  return `<div class="grid"><section class="card col-12"><div class="row between" style="margin-bottom:12px"><div><h2>📒 Історія</h2><div class="muted">Заправки, поїздки й ручні корекції в хронологічному порядку, погруповані по днях.</div></div><div class="row"><button class="btn secondary" data-action="openGasRefuel">+ Газ</button><button class="btn secondary" data-action="openPetrolRefuel">+ Бензин</button><button type="button" class="btn danger" data-action="clearAllHistory">Видалити всю історію</button></div></div>${visibleDays.length?visibleDays.map(d=>{const dayKm=groups[d].filter(x=>x.type==='trip').reduce((a,x)=>a+parseNum(x.km),0);const kmLabel=dayKm>0?` · ${fmt(dayKm,1)} км`:'';return `<section class="date-group"><div class="date-title">${dateLabel(d)} <span>${groups[d].length} запис(и)${kmLabel}</span></div><div class="list">${groups[d].map(historyEventHtml).join('')}</div></section>`}).join(''):`<div class="muted">Історія порожня.</div>`}${hasMore?`<div class="hist-load-more"><button data-action="historyLoadMore">⬆️ Завантажити ще 2 тижні</button></div>`:''}</section></div>`;
}
function periodRange(){const now=new Date(); if(state.statsPeriod==='custom')return {from:state.statsFrom||today(),to:state.statsTo||today()}; if(state.statsPeriod==='year')return {from:`${now.getFullYear()}-01-01`,to:`${now.getFullYear()}-12-31`}; if(state.statsPeriod==='month')return {from:new Date(now.getFullYear(),now.getMonth(),1).toISOString().slice(0,10),to:new Date(now.getFullYear(),now.getMonth()+1,0).toISOString().slice(0,10)}; const day=now.getDay()||7, monday=new Date(now); monday.setDate(now.getDate()-day+1); return {from:monday.toISOString().slice(0,10),to:today()}}
function statsPage(){const {from,to}=periodRange(); const trips=state.trips.filter(t=>(t.date||'')>=from&&(t.date||'')<=to); const by={}; trips.forEach(t=>{const d=t.date||today(); by[d]||(by[d]={city:0,road:0}); by[d][t.mode==='road'?'road':'city']+=parseNum(t.km)}); const days=Object.keys(by).sort(); const totalCity=trips.filter(t=>t.mode!=='road').reduce((a,t)=>a+parseNum(t.km),0), totalRoad=trips.filter(t=>t.mode==='road').reduce((a,t)=>a+parseNum(t.km),0), max=Math.max(1,...days.map(d=>by[d].city+by[d].road)); return `<div class="grid"><section class="card col-12"><h2>📊 Статистика пробігу</h2><div class="muted">🟡 місто · 🔵 траса. Статистика рахується тільки по фактично доданих поїздках. Період: ${from} — ${to}.</div><div class="row" style="margin:12px 0"><button class="mini-btn" data-action="setStats" data-value="week">Тиждень</button><button class="mini-btn" data-action="setStats" data-value="month">Місяць</button><button class="mini-btn" data-action="setStats" data-value="year">Рік</button><button class="mini-btn" data-action="setStats" data-value="custom">Свій</button></div>${state.statsPeriod==='custom'?`<div class="form-grid"><div><label>Від</label><input id="statsFrom" type="date" value="${state.statsFrom||today()}"></div><div><label>До</label><input id="statsTo" type="date" value="${state.statsTo||today()}"></div><div class="full"><button class="btn secondary full" data-action="applyStatsCustom">Застосувати</button></div></div>`:''}<div class="stats"><div class="stat city"><div class="value">${fmt(totalCity,1)} км</div><div class="label">місто</div></div><div class="stat road"><div class="value">${fmt(totalRoad,1)} км</div><div class="label">траса</div></div><div class="stat"><div class="value">${fmt(totalCity+totalRoad,1)} км</div><div class="label">разом</div></div></div><div style="margin-top:18px">${days.length?days.map(d=>{const city=by[d].city,road=by[d].road,total=city+road;return `<div class="bar-row"><div><b>${new Date(d+'T12:00:00').toLocaleDateString('uk-UA',{day:'2-digit',month:'2-digit'})}</b></div><div class="bar-track"><div class="bar-city" style="width:${city/max*100}%"></div><div class="bar-road" style="width:${road/max*100}%"></div></div><div class="muted">${fmt(total,1)} км</div></div>`}).join(''):`<div class="muted">За цей період поїздок ще немає.</div>`}</div></section></div>`}
function settingsPage(){return `<div class="grid"><section class="card col-6"><h2>⚙️ Налаштування</h2><div class="form-grid"><div><label>Витрата місто, л/100</label><input id="setGasCity" inputmode="decimal" value="${state.settings.gasCity}"></div><div><label>Витрата траса, л/100</label><input id="setGasRoad" inputmode="decimal" value="${state.settings.gasRoad}"></div><div><label>Місткість бака газ, л</label><input id="setGasCap" inputmode="decimal" value="${state.settings.gasTankCapacity}"></div><div><label>Місткість бака бензин, л</label><input id="setPetrolCap" inputmode="decimal" value="${state.settings.petrolTankCapacity}"></div><div><label>Поріг, %</label><input id="setWarnPercent" inputmode="decimal" value="${state.settings.gasWarningPercent}"></div><div><label>Поріг, км</label><input id="setWarnKm" inputmode="decimal" value="${state.settings.gasWarningKm}"></div><div><label>Остання ціна газу</label><input id="setGasPrice" inputmode="decimal" value="${state.settings.lastGasPrice}"></div><div><label>Остання ціна бензину</label><input id="setPetrolPrice" inputmode="decimal" value="${state.settings.lastPetrolPrice}"></div><div class="full"><button class="btn full" data-action="saveSettings">Зберегти налаштування</button></div></div></section><section class="card col-6"><h2>💾 Backup</h2><p class="muted">Дані зберігаються локально в браузері. Для перенесення між телефоном і комп’ютером експортуй JSON і імпортуй його на іншому пристрої.</p><div class="row"><button class="btn secondary" data-action="exportBackup">Експорт backup</button><button class="btn secondary" data-action="exportCsv">Експорт CSV</button><button class="btn secondary" data-action="importBackup">Імпорт backup</button></div><h3>Небезпечна зона</h3><button class="btn danger" data-action="resetAll">Скинути все</button></section></div>`}
function handleAction(a,id,v){if(a==='editBalance')openBalanceModal(v);if(a==='openGasRefuel')openRefuelModal('gas');if(a==='openPetrolRefuel')openRefuelModal('petrol');if(a==='openPetrolLevel')openPetrolLevel();if(a==='deletePetrolLevel')deletePetrolLevel(id);if(a==='setPetrolBar'){setLevelBar(parseInt(v));}if(a==='recalcPetrolKmpb'){render();toast('Перераховано — '+((petrolKmPerBar()||0))+' км/паличку');}if(a==='resetPetrolStats')resetPetrolStats();if(a==='recalcPetrolKmpb')recalcPetrolKmpb();if(a==='resetPetrolStats')resetPetrolStats();if(a==='addCalcSegment')addCalcSegment();if(a==='deleteCalcSegment')deleteCalcSegment(id);if(a==='copyCalcSegment')copyCalcSegment(id);if(a==='editCalcSegment')editCalcSegment(id);if(a==='clearCalc')clearCalc();if(a==='updateCalcOptions')updateCalcOptions();if(a==='addTrip')addTrip();if(a==='deleteTrip')deleteTrip(id);if(a==='editTrip')editTrip(id);if(a==='quickTrip')openQuickTripModal();if(a==='scrollTrips')scrollToTrips();if(a==='clearTrips')clearTrips();if(a==='editEntry')editEntry(id);if(a==='deleteEntry')deleteEntry(id);if(a==='editCorrection')editCorrection(id);if(a==='deleteCorrection')deleteCorrection(id);if(a==='saveSettings')saveSettingsFromInputs();if(a==='saveGasPrice')saveGasQuickPrice();if(a==='clearAllHistory')clearAllHistory();if(a==='setDashboardMode'){state.settings.dashboardMode=v;saveState();render()}if(a==='exportBackup')exportBackup();if(a==='exportCsv')exportCsv();if(a==='importBackup')document.getElementById('importFile').click();if(a==='resetAll')resetAll();if(a==='goHistory'){_historyWeeksShown=2;setTab('history')}if(a==='historyLoadMore'){_historyWeeksShown+=2;document.getElementById('tab-history').innerHTML=historyPage();}if(a==='setStats'){state.statsPeriod=v;saveState();render()}if(a==='applyStatsCustom'){state.statsFrom=document.getElementById('statsFrom').value||today();state.statsTo=document.getElementById('statsTo').value||today();saveState();render()}}
function bindDynamicEvents(){['calcRoundTrip','calcShared','calcPassengers','calcIncludeDriver','calcPrice'].forEach(id=>{const el=document.getElementById(id); if(el)el.addEventListener('change',updateCalcOptions)}); document.querySelectorAll('[data-action]').forEach(el=>{})}
function bindFormattedInputs(){document.querySelectorAll('[data-format="mileage"]').forEach(el=>{if(el.dataset.fmtBound)return;el.dataset.fmtBound='1';const format=()=>{const v=parseNum(el.value);if(v){el.value=mileageFmt(v)}};el.addEventListener('input',()=>{const v=parseNum(el.value);if(v){el.value=mileageFmt(v);try{el.setSelectionRange(el.value.length,el.value.length)}catch(e){}}});el.addEventListener('blur',format)})}
function scrollToTrips(){setTab('gas');setTimeout(()=>document.getElementById('quickTripsBlock')?.scrollIntoView({behavior:'smooth',block:'start'}),80)}
function openQuickTripModal(){const s=gasStats(); document.getElementById('modalRoot').innerHTML=`<div class="modal-backdrop" data-modal-close><div class="modal" onclick="event.stopPropagation()"><div class="modal-head"><h2>Швидко додати поїздку</h2><button class="mini-btn" data-modal-close>Закрити</button></div><div class="form-grid"><div>${labelHelp('Дата','Дата фактичної поїздки. За замовчуванням — сьогодні.')}<input id="qTripDate" type="date" value="${today()}"></div><div>${labelHelp('Кілометри','Фактична відстань. Піде у статистику й спише газ із балансу.')}<input id="qTripKm" inputmode="decimal" placeholder="20"></div><div>${labelHelp('Режим','Місто або траса. Від цього залежить витрата газу.')}<select id="qTripMode"><option value="city">Місто</option><option value="road">Траса</option></select></div><div><label>Поточний пробіг</label><input value="${mileageFmt(s.odometer)} км" disabled></div><div class="full">${labelHelp('Коментар','Необов’язково: куди їздив або що це була за поїздка.')}<textarea id="qTripComment" placeholder="Наприклад: магазин, робота, по справах"></textarea></div><button class="btn secondary" data-modal-close>Скасувати</button><button class="btn good" id="saveQuickTripBtn">Додати</button></div></div></div>`; document.querySelectorAll('[data-modal-close]').forEach(x=>x.onclick=closeModal); document.getElementById('saveQuickTripBtn').onclick=()=>{const km=parseNum(document.getElementById('qTripKm').value), mode=document.getElementById('qTripMode').value, date=document.getElementById('qTripDate').value||today(), comment=document.getElementById('qTripComment').value.trim(); if(km<=0)return toast('Введи кілометри'); closeModal(); addTripRecord({km,mode,date,comment})}}
function openBalanceModal(kind,corr=null){editingCorrectionId=corr?.id||null; const c=current(); const label=kind==='gas'?'газу в баку':kind==='petrol'?'бензину в баку':'поточний пробіг'; const value=corr?corr.value:(kind==='gas'?c.gas:kind==='petrol'?c.petrol:c.odometer); const unit=kind==='odometer'?'км':'л'; document.getElementById('modalRoot').innerHTML=`<div class="modal-backdrop" data-modal-close><div class="modal" onclick="event.stopPropagation()"><div class="modal-head"><h2>Змінити ${label}</h2><button class="mini-btn" data-modal-close>Закрити</button></div><div class="form-grid"><div>${labelHelp('Дата','Дата, на яку вноситься ручна корекція.')}<input id="balDate" type="date" value="${esc(corr?.date||today())}"></div><div>${labelHelp(`Нове значення, ${unit}`,'Фактичний баланс або пробіг. Для пробігу програма може запропонувати додати різницю в статистику.')}<input id="balValue" ${kind==='odometer'?'data-format="mileage" inputmode="numeric"':'inputmode="decimal"'} value="${kind==='odometer'?mileageFmt(value):fmt(value,1)}"></div><div class="full">${labelHelp('Коментар','Необов’язково: коротко поясни, чому робиш корекцію.')}<textarea id="balComment" placeholder="Наприклад: уточнив за датчиком / фактичний пробіг">${esc(corr?.comment||'')}</textarea></div><button class="btn secondary" data-modal-close>Скасувати</button><button class="btn good" id="saveBalBtn">Зберегти</button></div></div></div>`; bindFormattedInputs(); document.querySelectorAll('[data-modal-close]').forEach(x=>x.onclick=closeModal); document.getElementById('saveBalBtn').onclick=()=>saveBalance(kind)}
async function saveBalance(kind){const val=kind==='odometer'?Math.round(parseNum(document.getElementById('balValue').value)):round2(parseNum(document.getElementById('balValue').value)); const date=document.getElementById('balDate').value||today(), comment=document.getElementById('balComment').value.trim(); if(val<0)return toast('Значення не може бути відʼємним'); if(editingCorrectionId){state.corrections=state.corrections.map(c=>c.id===editingCorrectionId?{...c,kind,value:val,date,comment,updatedAt:new Date().toISOString()}:c); closeModal();saveState();render();return toast('Корекцію збережено')} if(kind==='odometer'){const baseline=odometerAtDate(date); const diff=val-baseline; closeModal(); if(diff>0){const choice=await askOdoDiff(diff,baseline,val); if(choice==='city'||choice==='road'){addTripRecord({km:diff,mode:choice,date,comment:comment||'Додано з ручної зміни пробігу'}); return} } addCorrection(kind,val,date,comment); } else {closeModal(); addCorrection(kind,val,date,comment)} }
function addCorrection(kind,value,date=today(),comment=''){state.corrections.push({id:uid(),kind,value,date,comment,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()}); saveState();render();toast('Баланс оновлено')}
function addTripRecord({km,mode,date,comment,id=null}){state.trips.push({id:id||uid(),date:date||today(),km:round1(km),mode:mode||'city',comment:comment||'',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()}); saveState();render();toast('Поїздку додано')}
function addTrip(){const km=parseNum(document.getElementById('tripKm')?.value), mode=document.getElementById('tripMode')?.value||'city', date=document.getElementById('tripDate')?.value||today(), comment=document.getElementById('tripComment')?.value.trim()||''; if(km<=0)return toast('Введи кілометри'); addTripRecord({km,mode,date,comment})}
function openTripModal(t){editingTripId=t.id; document.getElementById('modalRoot').innerHTML=`<div class="modal-backdrop" data-modal-close><div class="modal" onclick="event.stopPropagation()"><div class="modal-head"><h2>Редагувати поїздку</h2><button class="mini-btn" data-modal-close>Закрити</button></div><div class="form-grid"><div><label>Дата</label><input id="eTripDate" type="date" value="${esc(t.date||today())}"></div><div><label>Кілометри</label><input id="eTripKm" inputmode="decimal" value="${fmt(t.km,1)}"></div><div class="full"><label>Режим</label><select id="eTripMode"><option value="city" ${t.mode!=='road'?'selected':''}>Місто</option><option value="road" ${t.mode==='road'?'selected':''}>Траса</option></select></div><div class="full"><label>Коментар</label><textarea id="eTripComment">${esc(t.comment||'')}</textarea></div><button class="btn secondary" data-modal-close>Скасувати</button><button class="btn good" id="saveTripBtn">Зберегти</button></div></div></div>`; document.querySelectorAll('[data-modal-close]').forEach(x=>x.onclick=closeModal); document.getElementById('saveTripBtn').onclick=saveTrip}
function editTrip(id){const t=state.trips.find(x=>x.id===id); if(t)openTripModal(t)} function saveTrip(){state.trips=state.trips.map(t=>t.id===editingTripId?{...t,date:document.getElementById('eTripDate').value||today(),km:round1(parseNum(document.getElementById('eTripKm').value)),mode:document.getElementById('eTripMode').value,comment:document.getElementById('eTripComment').value.trim(),updatedAt:new Date().toISOString()}:t); closeModal();saveState();render();toast('Поїздку збережено')} async function deleteTrip(id){const ok=await askConfirm('Видалити поїздку?','Цей запис буде прибрано зі статистики, а баланс газу й пробіг перерахуються.','Видалити','Скасувати','danger'); if(!ok)return; state.trips=state.trips.filter(t=>t.id!==id); saveState();render();toast('Поїздку видалено')} async function clearTrips(){const ok=await askConfirm('Очистити всі поїздки?','Заправки залишаться, але статистика пробігу і розрахований баланс газу зміняться.','Очистити','Скасувати','danger'); if(!ok)return; state.trips=[];saveState();render();toast('Поїздки очищено')}
function openRefuelModal(fuel,entry=null){editingEntryId=entry?.id||null; const isGas=fuel==='gas', price=entry?entry.price:(isGas?state.settings.lastGasPrice:state.settings.lastPetrolPrice), suggested=entry?entry.mileage:current().odometer, litersDefault=entry?entry.liters:''; document.getElementById('modalRoot').innerHTML=`<div class="modal-backdrop" data-modal-close><div class="modal" onclick="event.stopPropagation()"><div class="modal-head"><h2>${entry?'Редагувати':'Додати'} ${isGas?'газову':'бензинову'} заправку</h2><button class="mini-btn" data-modal-close>Закрити</button></div><div class="form-grid"><div>${labelHelp('Дата','Дата заправки. За замовчуванням — сьогодні.')}<input id="mDate" type="date" value="${esc(entry?.date||today())}"></div><div>${labelHelp('Пробіг, км','Фактичний пробіг на момент заправки. Якщо він більший за поточний, програма оновить загальний пробіг.')}<input id="mMileage" data-format="mileage" inputmode="numeric" value="${suggested?mileageFmt(suggested):''}" placeholder="${suggested?mileageFmt(suggested):'158 303'}"></div><div>${labelHelp('Літри','Скільки фактично залив. Якщо повний бак вимкнено — ці літри додадуться до балансу.')}<input id="mLiters" inputmode="decimal" value="${esc(litersDefault)}" placeholder="${isGas?'32':'10'}"></div><div>${labelHelp('Ціна за літр','Ціна з цієї заправки. Вона стане останньою ціною для наступних записів.')}<input id="mPrice" inputmode="decimal" value="${esc(price)}" placeholder="40,9"></div><div class="full">${labelHelp('Коментар','Необов’язкова нотатка до заправки.')}<textarea id="mComment" placeholder="Наприклад: ОККО, повний балон, по дорозі додому">${esc(entry?.comment||'')}</textarea></div><div class="full checkline"><input id="mFull" type="checkbox" ${entry?(entry.fullTank?'checked':''):(isGas?'checked':'')}><span>Повний бак</span></div><div class="full"><div class="warning" id="mSumPreview">Сума: 0 грн</div></div><button class="btn secondary" data-modal-close>Скасувати</button><button class="btn good" id="saveRefuelBtn">Зберегти</button></div></div></div>`; bindFormattedInputs(); const up=()=>{document.getElementById('mSumPreview').textContent='Сума: '+money(parseNum(document.getElementById('mLiters').value)*parseNum(document.getElementById('mPrice').value))}; ['mLiters','mPrice'].forEach(id=>document.getElementById(id).addEventListener('input',up)); up(); document.querySelectorAll('[data-modal-close]').forEach(x=>x.onclick=closeModal); document.getElementById('saveRefuelBtn').onclick=()=>saveRefuel(fuel)}

